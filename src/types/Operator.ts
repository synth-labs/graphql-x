enum Operator {
    EQUAL = '=',
    NOT_EQUAL = '!=',
    GREATER_THAN = '>',
    GREATER_THAN_OR_EQUAL = '>=',
    LESS_THAN = '<',
    LESS_THAN_OR_EQUAL = '<=',
    IS_NULL = 'IS NULL',
    IS_NOT_NULL = 'IS NOT NULL',
    LIKE = 'LIKE'
}

export default Operator;